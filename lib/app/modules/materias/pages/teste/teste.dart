import 'package:flutter/material.dart';

class Gabriel extends StatelessWidget {
  const Gabriel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
